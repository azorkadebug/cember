{
  "name": "cember-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore güvenlik kurallarının emülatör testleri",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-cember \"node --test rules.test.mjs\"",
    "csp-hash": "node csp-hash.mjs",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
